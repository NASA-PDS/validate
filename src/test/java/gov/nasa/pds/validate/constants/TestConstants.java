package gov.nasa.pds.validate.constants;

import java.io.File;

public final class TestConstants {

    public static final String TEST_DATA_DIR = System.getProperty("user.dir") + File.separator + "src/test/resources"; 
    public static final String TEST_OUT_DIR = System.getProperty("user.dir") + File.separator + "target/test";
    public static final String EXAMPLE_DIR = System.getProperty("user.dir") + File.separator + "src/main/resources/examples";
    public static final String RESOURCES_DIR = System.getProperty("user.dir") + File.separator + "src/main/resources/util";
}

